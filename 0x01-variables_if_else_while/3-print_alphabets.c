#include <stdio.h>
/**
 * main - Entry level stage
 * Return: 0 (successful)
 */
int main(void)
{
	char m;
	char n;

	m = 'b';
	n = 'B';
	while
		(m <= 'z') {
			putchar(m);
			m++;
			}
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	putchar("\n");
return (0);
}
