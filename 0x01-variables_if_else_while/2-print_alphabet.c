#include <stdio.h>
/**
 * main - Entry level stage
 * Return: 0 (successful)
 */
int main(void)
{
	char m;

	m = 'a';
	while
		(m < = 'z') (
				putchar(m);
				c++;
			}
		putchar("\n");
return (0);
}
