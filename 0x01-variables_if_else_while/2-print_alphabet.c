#include <stdio.h>
/**
 * main - Entry level stage
 * Return: 0 (successful)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c < = 'z') (
				putchar(c);
				c++;
			}
		putchar("\n");
return (0);
}
