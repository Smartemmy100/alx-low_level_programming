#include "main.h"

/**
 * main - check the code
 * Return: 0 (successful)
      */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Asaac Asimov";
	print_rev(str);
return (0);
}
