#include "main.h"

/**
 * main - check the code
 *
 * Return: 0 (successful)
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Emmanuel Bayo";
	_puts(str);
return (0);
}
