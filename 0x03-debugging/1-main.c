#include <stdio.h>

/**
  * main - causes an inifinate loop
  * Return: 0
  */

int main(void)
{
	int i;

	printf("Infinate loop incoming :(\n");

	/*
	*i = 0;
	*while (i < 10)
	*{
	*putchar(i);
	}
	 */
	printf("Infinite loop avoided! \\o/\n");

return (0);
}
